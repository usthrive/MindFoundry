# Best Brains — handoff, 2026-08-07

Written at the end of the Level-C prose-pass session. Everything below is on branch
`best-brains-content-engine`, **committed but NOT merged** (merging deploys; the owner
gates that). Read HANDOFF-2026-07-30.md first for the program frame.

---

## 1. What this session shipped

**Level C is done: 24/24 weeks at 0.00% on the sentence law AND 24/24 clean ACCEPT
from the reader evaluation** (pre-repair 21/24 = 87.5%; the three non-ACCEPTs were
repaired and re-judged ACCEPT at 4.64–4.71). Corpus health after everything:
19,782 assertions 0 failures · QG-11/12 pass · entropy 0 guessable mastery slots ·
cross-week `--strict` clean · `tsc` clean · readability B and C both PASS.

1. **The readability gate was under-measuring (L44)** — it exempted
   `hook`/`whyBeforeHow`/`summary` as adult surfaces, but LessonRoom SPEAKS all
   three. Gate fixed; the honest gate then failed all 24 C weeks (13.8–44.9%) and
   **11 live B weeks** (2.3–4.4%).
2. **Level C prose pass** — 3 agent batches × 8 weeks, ~1,000 template sentences
   split to the b13/b20/b21 register. All 24 weeks measure exactly 0.00%.
3. **Level B repair** — the 11 newly-red weeks' explanation blocks fixed (24
   strings, 90 sentences); untouched weeks verified bit-identical.
4. **Shared-lib prose fixes**: `items.ts` subWhole hint split; `metacog.ts`
   withCheckBack/withReasonableness colon-welds → closed lead sentences (the L40
   class, on the two siblings the L40 fix missed); `stats.ts` graph prompts/hints
   split (serves B23+C23); `items.ts` compareWhole equal-draw rationale "agree in
   all six" → "agree in every place" (was false for 2–3-digit draws).
5. **Reader evaluation** (6 judges + 1 re-judge, 2 seeds/week, ~2,900 hand
   recomputations): **zero wrong numbers anywhere in Level C**. All findings were
   claim-level (L47). Repairs applied and re-verified:
   - c09/c10 sprints: `[2,10]` served untaught ×6–×9 facts in a TIMED sprint
     (G8 forward leak) → taught-only `[2,5]` + honest labels (L46);
   - c12's false core rule ("number of groups" vs what one group holds) fixed at
     6 sites + header (L47);
   - c13/c14 sprints: dead `{tables:…}` params → explicit honest `factorRange`;
   - isomorphNotes freshness overclaim (false in 12/18 weeks) → honest wording, 12 files;
   - c05 GE story shape, c13 orientation-unsafe split rule, c14 dangling "one more
     egg" line, c11/c12 spoken-opener echo, c17 degenerate EA draw (deterministic
     guard), c22/c24 isomorphNotes, c23 header claim, c24 sprint division-label.
6. **New tool**: `frontend/scripts/bb-dump-pack.ts` — dumps a generated pack as
   JSON; the reader eval judges generated packs, never source.
7. Kit §E2.9 updated (probe budget is now the full ≤15 words — closed leads).

## 2. Validator facts learned the hard way

- **DD11**: sprint `sourceWeek` must be ≥2 weeks before the current week. c09's
  taught-only range spans C7+C8 facts but must declare C7; disclosed in a comment.
- The §6.9 preflight requires `conceptualAnchor` verbatim + a causal connective in
  the first 60% of `whyBeforeHow` — any prose restructuring must re-seed both.
- Rung-1 hints must end in "?" (§6.5).
- Splitter: em-dash/colon/semicolon/`."` do NOT end sentences; interpolations
  (ternaries, joins, multi-word scene nouns) hide words from source-level counts.

## 3. Fix queue (design decisions — owner input wanted)

1. **Level-B `whyBeforeHow` register (L44)**: 8 of 11 repaired weeks speak ADULT
   framing to the child ("Ask a six-year-old…"). Either re-point the prose at the
   child or stop LessonRoom speaking whyBeforeHow. Level C is clean — B-specific.
2. **GE-echo guard gaps** (bit C21 at both seeds, and C23's mastery form):
   `numericTokens` includes `[image:…]` alt numerals (surface.ts:20-23) so
   figure-bearing items never match their GE; the redraw wrap covers only
   `dayDrafts` (assemble.ts:190-198) — mastery drafts are unprotected, and one
   lesson-solved problem sits inside the 85% gate (C23-MA-03 ≡ GE-04, seed 11).
   Fixing changes draws corpus-wide — needs its own gated pass.
3. **Form-B freshness guard** compares only against Form A (assemble.ts:221-232);
   Form/daily collisions are what falsified isomorphNotes. Either enforce or keep
   the now-honest wording.
4. **Sprint realizer**: reject unknown params; add a label-vs-servable-set check
   (L46). Consider a table-aware mult template so "×2, ×5, ×10" is expressible.
5. **Week-dependent stream seeds** (L45): `streamRng(packSeed,'fs')` et al. are
   week-independent → identical sprints/warm-up collisions across weeks at a fixed
   seed. Latent (per-week random seeds mask it) — do not let anyone "stabilize"
   pack seeds across weeks without fixing this first.
6. **ASN capture gap** (all C weeks): Day-5 ASN items demand "say in one sentence
   how you know" but capture choice-key only. Add a text field or drop the demand.
7. **c07/c08 duplicated bare ASN** (handoff-07-30 §6.3 debt, confirmed): both
   weeks serve the identical ×2-vs-+2 claim; c08's header says C8 owns the
   mid-chain version. Needs a replacement ASN authored for c08.
8. Smaller: c19's three degenerate draws (estimate-tie ~1000g; basket answer =
   untouched operand ×2); c12 pizza ×11/×12 band-stretch (`int(3,9)` suggested);
   C10 puzzle vs multi-clue signature (W7=3); c22 discrimNameTilted served 2×/pack;
   erroranalysis.ts:109 model-answer wording for slip-type items; C2 puzzle static
   across seeds; `d_pv_expand_v1` degenerate "100 = ?" draw; C9 ribbon-tape
   GE-vs-item reading mismatch (W8 note).

## 4. Reader-eval protocol (reusable for Levels A/D/E)

Judge GENERATED packs at two INDEPENDENT seeds (11 + 33; **not 202** — correlated
with 11, L45). Per week: recompute every number by hand AND apply every stated
rule to the pack's own examples checking referents (L47); check sprint
label-vs-servable set (L46); test isomorphNotes literally; compare assessed items
against GEs (the guard is blind, §3.2); read the register (who does the prose talk
to). Batch 4 weeks/judge; ~330–500 recomputations per batch. ACCEPT bar per
CONTENT-STYLE-GATE.md; ≥80% clean ACCEPT per level before ship.

## 5. Next work, in order (unchanged from 07-30 otherwise)

1. **Level A** (2/24; blocked on nothing — the figure renderer landed in B1.0).
   A's pedagogy differs most; read FILL-ARCHITECTURE §3 first.
2. Residual Level B items (07-30 §6) + the B register decision (§3.1 above).
3. Levels D and E.
