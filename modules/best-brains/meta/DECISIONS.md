# DECISIONS.md — Best Brains Module Pipeline

Every judgment call the orchestrator makes, with rationale. Newest at bottom.

| # | Date | Phase | Decision | Rationale |
|---|------|-------|----------|-----------|
| D1 | 2026-07-19 | 0 | Module folder placed at `MindFoundry/modules/best-brains/` (inside the git repo), not at `Math_Tutor/modules/` | Meta-prompt says "repo root"; the git repo root is `MindFoundry/`. Keeps artifacts versioned with the code they describe. |
| D2 | 2026-07-19 | 0 | Skipped a fresh codebase survey pass | Orchestrator already performed a full survey of the Kumon module (structure, generators, session flow, Supabase schema, roadmap) immediately before pipeline start in this same session. Findings are logged in PROGRESS.md; a repeat pass would burn timebox for no new information. |
| D3 | 2026-07-19 | 1 | Phase 1 uses 6 parallel general-purpose research sub-agents, one per source category, each writing its own raw-notes file | Matches §4's category split exactly; parallelism fits the 60-min timebox; per-source raw notes satisfy the inventory format requirement. |
| D4 | 2026-07-19 | 1 | Research agents instructed to label every finding Confirmed/Inferred/Speculative + High/Med/Low confidence at capture time, not retroactively in Phase 2 | Cheaper to label at the moment of capture when the source is in front of the agent; Phase 2 then audits labels instead of inventing them. |
| D5 | 2026-07-19 | 1 | Ghana "Best Brain" exam-prep brand formally excluded; Scribd re-uploads of the 2020 official free workbooks treated as off-limits third-party copies | Disambiguation + the NEVER constraint on non-official copies; the 2020 material's *existence* (week-numbered packets) is still usable as a Confirmed-historical structural fact from official traces. |
| D6 | 2026-07-19 | 1 | Two long video interviews (founder/franchise) left unmined — transcripts network-blocked | Cost/benefit: manual viewing isn't possible for an autonomous agent; flagged as optional Phase 3 target only if a top gap depends on them. |
| D7 | 2026-07-19 | 2 | Phase 2 run as a single analyst sub-agent (no new web research) producing ledger rows with stable IDs (E1…) and gap rows (G1…) | Ledger is a synthesis task over 840 lines of notes — one coherent view beats parallel partial views; stable IDs enable the §7 traceability requirement ("per E12") in Phases 4–6. |
| D8 | 2026-07-19 | 3 | Phase 3 scoped to exactly G1–G5 with named attack vectors per gap (Wayback, transcript mirrors, marketplace covers, indexed employee reviews, official article corpus); G6–G14 sent straight to Design Defaults | Meta-prompt §6: targeted searches only. Attack-vector specificity is what made Wayback pay (recovered the 2013–2019 level ladder). |
| D9 | 2026-07-19 | 3 | The historical (2013–2019) level ladder is accepted as design-relevant evidence even though the modern naming ("Level M1") may differ | The ladder's *structure* (levels ≈ grades, 26 weeks/level, a/b booklet splits, Pre-Algebra/Algebra endpoints) is the design-relevant part and is Confirmed; the naming discrepancy is quarantined as its own Speculative row + Design Default rather than blocking. |
| D10 | 2026-07-19 | 3 | Module will deliberately DIVERGE from Best Brains on two digital points, to be flagged in the ledger-traceable design: immediate item-level feedback (vs their weekly-batch human grading) and systematic spaced review (vs their failure-triggered-only review) | Their weekly-batch grading is an artifact of paper + human graders, not pedagogy; their own official copy endorses "weekly repetition with increasing complexity". Divergences will be explicit `[original design]` rows so authenticity reviewers in Phase 7 can judge them. |
| D11 | 2026-07-19 | 4 | Curriculum map follows the meta-prompt's mandated 5-level A–E progression; DD2's recovered 16×26 ladder informs it as the structural spine (level≈grade anchoring, fixed-length week sequence per level, a/b half-level pacing idea) rather than being copied | Meta-prompt §7.2 is explicit (5 levels A–E) and outranks a Design Default; also, copying BB's actual 16-level ladder verbatim would edge toward reproducing their sequence, which the NEVER constraints forbid. The 26-week rhythm survives as "each level = a fixed sequence of weekly concepts". |
| D12 | 2026-07-19 | 4 | Phase 4 split into two sequential sub-agents: (A) METHODOLOGY-MODEL + CURRICULUM-MAP, then (B) TEACHER-PERSONA + QUESTION-GENERATOR-SPEC reading A's outputs | The four artifacts form a dependency chain (loop → map → persona → generator). Two sequential agents keep each within output limits while preserving coherence; parallel agents would fork the methodology. |
| D13 | 2026-07-19 | 4 | Part B agent killed by a transient API server error before writing; resumed in place (context intact) with a write-each-file-as-ready instruction rather than relaunching fresh | Resume preserves the ~30 min of input reading; incremental writes bound the cost of a repeat failure. |
| D14 | 2026-07-19 | 4 | External deep-research report (user drop, found at `docs/Best Brains Math Program and Method.md`) copied into `external-inbox/` and reconciled mid-Phase-4 as ledger revision **P3.1** (E102–E106 + confidence upgrades) instead of waiting for the Phase 5 boundary | Inbox protocol says ingest on arrival; the report contained one Confirmed/High detail (E102 Progress Book field structure) that directly improves an artifact currently being written (parent-summary template) — waiting would have meant reworking it. Zero contradictions found; nothing quarantined. The running Part B agent was messaged to incorporate E102/E103. |
| D15 | 2026-07-19 | 4 | User's inbox drop landed in `MindFoundry/docs/` rather than the inbox folder; file was COPIED (not moved) into the inbox with a dated name, original left untouched | Don't relocate user-placed files without being asked; the inbox copy is the pipeline's working artifact. (Also logged as a v2 learning: inbox path must be surfaced more prominently.) |
| D17 | 2026-07-19 | 6 | Phase 6 will commit at each working increment per the meta-prompt's explicit instruction, overriding the standing explain-before-commit rule for this pipeline only; commits are pathspec-only (module + module docs paths), exact contents logged in PROGRESS.md per increment; repo's pre-existing untracked QA/video files are never swept in | The meta-prompt is the user's durable authorization ("commit at each working increment", "do not stop to ask"); the pathspec-only discipline and content logging preserve the intent of the standing rule. Main stays buildable at every commit (live Netlify deploy). |
| D18 | 2026-07-19 | 6 | Build order: (1) Module Interface Contract + data model/migration, (2) pack generator + seed content (3 levels × 2 weeks min), (3) child session flow, (4) mastery/feedback + parent view, (5) integration (module card, lazy routes). Sequential builder sub-agents, one per increment, TypeScript+build check before each commit | Matches the meta-prompt's prescribed incremental order; sequential builders prevent merge conflicts in shared files (App.tsx, contract types); the contract lands first because everything else implements it. |
| D19 | 2026-07-19 | 6 | Fourth external drop (learning-science literature review) routed to a **Design-Defaults addendum** (`DESIGN-DEFAULTS-ADDENDUM-LS1.md`), NOT ledger rows | It contains no Best Brains facts — it audits our defaults against research. Verdict: nothing contradicted; 6 refinements adopted (LS1-R1 age-banded caps, R2 adaptive stop, R3 hint-ladder attempt-gate + fix-it, R4 weekly retrieval ramp, R5 mastery stability rule DD1.1, R6 bounded choice), 2 deferred (multi-day 3-opportunity gate; adaptive 5-rung fade). LS1-R3 messaged to the in-flight increment-3 builder; R1/R2/R4/R5 assigned to increments 4–5; R6 post-MVP. |
| D21 | 2026-07-20 | 7 | Session model switched Fable 5 → Opus 4.8 [1m] for the Phase 7 fix loop + bookkeeping (user-initiated, orchestrator-recommended). Plan: switch back to Fable 5 for the Phase 7c meta-prompt v2 write only (dense cross-run synthesis = the one high-value deliverable left). | The remaining work executes against finished specs (mechanical); the v2 template compounds across all future program runs and warrants the stronger model. Orchestrator will explicitly signal the switch-back point. |
| D23 | 2026-07-20 | post-7 | User renamed the two module cards to the real methodology names — "Daily Practice"→**"Kumon Method"**, "Foundry Method"→**"Best Brains Method"** — in the shipped UI (registry displayName + card titles + parent header + dashboard banner). Flagged the trademark/affiliation consideration (the meta-prompt's NEVER-claim-affiliation rule); user's explicit call for this private/family build. Kept "Foundry Method" as an internal codename in code comments for one-line revert on any public/commercial release; module id stays `best-brains`. | User is the product owner overriding their own earlier constraint for private use; "Kumon Method"/"Best Brains Method" read as methodology labels (like "Montessori Method"). Revert path preserved so a public build can drop back to neutral names without rework. |
| D27 | 2026-07-21 | content-gen | **Generator REBUILT (Step 2); Level D re-passes BOTH gates at 100% (23/23 clean ACCEPT).** Executed the 9 fixes as a *generator/library* change, not per-week patches: a v2 pedagogy layer (13 seed-invariant preflight gates in `lib/pedagogy.ts` + new primitives `situations/multistep/discrimination/erroranalysis/metacog`), `validator.ts` QG-11 (anchor/claim audit), a `verifyFor` registry, and all 23 `weeks/dNN.ts` rewritten to `pedagogyContract:'v2'`, migration-gated by `V2_WEEKS`. Contract = `build/CONTENT-GENERATOR-FIX-SPEC.md` (Rev 2, hardened by a 4-lens adversarial review). Correctness GREEN (7,877 assertions), QG-11 regression pass, tsc clean, 120-seed sweep clean. Style gate (same rubric that failed the old corpus): 21/23 ACCEPT → fixed 2 borderlines (D2 unsolvable puzzle, D24 "review" word, `digitValue` ambiguity) → 23/23 = 100%. **Decision: Level D is DONE; the branch is now mergeable and the fill is unblocked — but merge/push/fill remain the user's call (R4/R6), and the Fable-5 checkpoint is owed.** | Two-gate architecture vindicated end-to-end: correctness proved answers right, the LLM style gate proved content authentic AND caught two defects the deterministic gate structurally cannot (a fraction chain whose answer matches its params but contradicts its prose; an authored puzzle that is arithmetically unsolvable). Exemplar-first (prove D4, then fan out) + adversarial spec review (7 blockers caught before any code) + seed-invariant gates (they run at pack-gen time) were the decisive process choices. From 1/24 to 23/23 without hand-authoring answers. |
| D26 | 2026-07-21 | content-gen | **Style gate applied to Level D → FAIL** (1/24 clean ACCEPT, 1 HUMAN_REVIEW, 22 REJECT; adversarial re-check overturned 11 of 12 scorer passes). Content is correctness-valid but "structurally polished, pedagogically hollow" — single-step noun-swap application items, missing multi-step word problems (G7, 18/24 weeks), no discrimination traps (W5), thin modeled fade (W2), no error-analysis (W7), + real correctness bugs outside the answerFor audit (W6/W8/distractor mislabels). **DECISION: do NOT merge `best-brains-content-engine` to main; do NOT proceed to the 111-week fill.** Next action = fix the GENERATOR/LIBRARY (9 concrete changes), regenerate Level D, re-run both gates, require ≥80% clean-ACCEPT before authorizing the fill. Results: `build/CONTENT-STYLE-GATE-RESULTS-LEVEL-D.md`. | The two-gate architecture worked exactly as intended — the correctness gate (2857 assertions, 0 failures) passed hollow content; the authenticity gate caught it BEFORE scaling to 111 weeks. This is the system succeeding, not failing. Hand-patching 24 weeks is wrong — it's one reproducible generator behavior. **Calibration CONFIRMED:** the hand-authored D17 fixture scored ACCEPT 4.93/4.8 (→ HUMAN_REVIEW after adversarial) — NOT rejected; decisive separation from the generated corpus (3.36–3.86). The gate is well-calibrated → trust the FAIL; generator is the target (one adversarial G7 Day-4-slot clause is slightly aggressive → minor tuning, changes no verdict). **Bonus finding:** the "correctness bugs" W6 (keys 29÷4=7R3, grades the correct 7R1 wrong) and W8 (fabricated 32×21=674) are REAL → the correctness validator QG-5 has COVERAGE HOLES (it audits computational keyed answers but not classification-item keys nor narrative/anchor numbers in prose/mistakeBank/hints/parent copy). Extend QG-5 accordingly (fix #6). |
| D25 | 2026-07-21 | content-gen | User asked "how are you confirming questions meet Best Brains STYLE?" — surfaced a real gap: generated content is correctness-gated (validator.ts QG-1..10) but NOT style-gated. Built a **Best Brains Authenticity/Style Gate** via a Workflow (11 agents: 6 ledger-source extractors → synthesis → 3-way adversarial stress-test + fixture calibration → finalize). Result: `build/CONTENT-STYLE-GATE.md` (10 hard gates BB-G1..10 + 14 weighted BB-W1..14; LLM-judge stage wired AFTER validator.ts; threshold = all hard gates pass + weighted mean ≥3.5 + depth-subset ≥3.0 + no-1s; else HUMAN_REVIEW). | Correctness ≠ authenticity; a validated pack can still be generic drill. The red-team's key catch: cluster items by cognitive-operation+step-count so representational-variety camouflage (one one-step problem in 5 costumes) fails BB-G5. Gate must be wired into the generation pipeline + run against the architect's proven level (integration pending architect delivery). Feeds the L17 v2 "engine phase". |
| D24 | 2026-07-20 | post-7 | Wrote two reference docs at `docs/methodology/`: `BEST-BRAINS-OVERVIEW.md` (standalone: time/day, items/day, weeks/level, concepts by level, our implementation) and `PROGRAM-COMPARISON.md` (Kumon vs Best Brains, table-driven, extensible to RSM/Mathnasium/Singapore). | User request; grounded in the frozen ledger + curriculum map (cited), not invented. Comparison doc scaffolded so each future program = one more column. |
| D22 | 2026-07-20 | 7 | Fix loop scoped from parent+teacher findings BEFORE student findings land: T-1 (teach-first gate), T-2 (hint-ladder reveal threshold vs authored rungs), P-1 (parent silent fetch-failure UI), P-5 (SchoolSync copy), + harness hardening (T-3) & re-runs. Student findings folded in on arrival before any fix is committed. | Two of three findings files suffice to start planning; committing fixes waits for the full set so nothing is reprioritized mid-flight. |
| D20 | 2026-07-20 | 6–7 | Browser-testing default switched (user request): Playwright headless driving system Chrome (`channel:'chrome'` → /usr/bin/google-chrome, no browser download) is the sanctioned tool for smoke tests and Phase 7 persona runs; chrome-devtools MCP demoted to opportunistic use when the user's live session is reachable. Headless auth via a clearly-named dedicated test account created through the app's own signup flow; only those rows may be mutated | chrome-devtools MCP depends on a WSL2 remote-debug bridge that dies with VM uptime (recurring known issue); Playwright is self-contained and reproducible for the Phase 7 harness. Documented in `testing/BROWSER-TESTING-TOOLING.md`; in-flight increment-4 builder messaged. |
| D16 | 2026-07-19 | 4 | Second external report (parent voice/competitors) reconciled as ledger revision **P3.2** (E107–E113). The Phase-1 "near-zero organic parent voice" finding was PARTIALLY REVISED rather than deleted (Birdeye Google-review mirrors are rich; forums remain thin); the ⚠E21 turnover contradiction was kept but nuanced (churn is employee-visible, largely parent-invisible) | Honest-revision principle: never silently delete a superseded finding — annotate it. The parent-invisibility nuance materially helps the module thesis (a digital teacher has perfect continuity where the real-world weak point is). E110 (RSM pressure anecdote) earmarked for the future RSM template run. |
| D28 | 2026-07-27 | Phase A (Fable 5) | **Phase-A judgment pass complete; four Phase-B contracts written** — `build/PEDAGOGY-CEILING-REVIEW.md` (verdict: depth is REAL; 7 ceiling lifts, chief = metacog wrapper narrates instead of elicits), `build/POLISH-PASS-SPEC.md` (L24's four surface classes + three NEW: grammar agreement "1 liters"/"a 8 cm", noun–predicate "ripe marbles", comma-less big numbers inside the place-value week), `build/FILL-ARCHITECTURE.md` (band-keyed GATE_PROFILE; 8 new generator families; 96 per-week recipes; honest R-list), `build/PHASE-B-BUILD-PLAN.md` (B0 polish→B1 families→fills E→C→B→A, each exemplar→wave→fan-out→≥80% gate). **Key architectural ruling: drawn-text surface scans go into a QG-12 VALIDATOR family (CI seed sweep); generation-time preflight stays blueprint-structural** — honors L19 seed-invariance while keeping "library fix + gate = both". Old-engine A1/A2/B1/B2/C1/C2 declared rebuild-in-fill, not shippable. No code written, nothing committed (Phase A is review-and-specify only; R5). | The three-phase model strategy working as designed: the top model spent where judgment compounds (what no rubric scores — who does the thinking, signal fade, problem tension) and on the specs Opus executes under gates. Fresh-eyes reading of REAL generated output (not source) surfaced three defect classes the L24 audit missed — re-confirming L24's "different reader finds different bugs" and adding: read the week whose CONCEPT is formatting (D1) when hunting formatting bugs. |
| D29 | 2026-07-27 | Phase B0 | **Figure renderer: gap found, DEFERRED to B1.0 (user-approved), not absorbed into the polish pass.** Investigating a user question about visual helpers surfaced that no component draws any figure (numbers in LEARNINGS L27 / FILL-ARCHITECTURE §2a); `FILL-ARCHITECTURE` Rev 1 had specified `figures.ts` as a data convention while wrongly assuming a renderer existed. Corrected the spec, added **B1.0 as the first Phase-B1 task, blocking every level fill**, scoped to ~9 parameterised SVG primitives driven by each item's own computed params. User decision: "fine with you moving forward, just make a note and come back to it" — so B0 continues uninterrupted and the renderer is picked up before any fill. | The gap is real but genuinely independent of the B0 surface fixes, so stopping B0 to build a renderer would have cost sequencing for nothing. It is a blocker for Level A specifically (a pre-reader cannot use an item whose picture is the question), which is why it must land before fills rather than during them. Recording it in four places (spec correction, build-plan task, learning, this row) because a note that lives only in conversation is a note that is lost. |
| D30 | 2026-07-27 | Phase B0 | **CONTENT_VERSION 1.2.0; Level-D surfaces deliberately NOT bit-stable with 1.1.0.** The B0 pass changes prose across most of Level D (currency, grammar, fractions, contexts, metacognition, error-analysis framing, thousands separators) while leaving every computed answer identical. Determinism is re-established at the new content (same seed → identical pack, verified), and `generator.params`/`answer.value` stay canonical so QG-5 re-derivation is untouched. Accepted rather than chasing bit-stability, because the whole point of the pass was to change what the child reads. | The prior version bump (1.0.0 → 1.1.0) was verified byte-identical; this one intentionally is not, so it is recorded explicitly rather than discovered later. Learners pinned to an older contentVersion keep regenerating their original packs (DD15 versioned pointers), so no in-flight learner's week changes under them. One consequence flagged for any future cutover and NOT yet verified: stored `bb_item_attempts` are keyed by positional item id, so attempts recorded at 1.1.0 map onto different items at 1.2.0 — harmless today (nothing is pushed, module not live) but a migration question before deploy. |
| D31 | 2026-07-27 | B0.8 | **Level D re-certified at CONTENT_VERSION 1.2.0: 23/23 clean ACCEPT after repair; the ≥80% bar is met and the fill stays unblocked.** First run measured 78.3% (18/23) with a deliberately harsher instrument — judges told to RECOMPUTE rather than read — which found two genuine answer-key errors, two clone puzzles and a leaking hint. All five repaired and re-cleared 5/5 by a fresh judge that verified each repair independently. Recorded caveat: the 100% combines two runs, since the 18 first-run ACCEPTs predate the advisory W3/W4 fixes those same judges recommended; the defensible claim is "every week ever flagged has been repaired and independently re-cleared." | The honest number mattered more than the round one. Reporting 78.3% first, repairing, then re-judging with a fresh judge is what makes the final figure mean something — and the two answer-key errors it surfaced (wrong mathematics that had passed both existing gates) are worth more than the score. Not actioned and recorded as authoring debt: Day-4 pages across most weeks are re-runs of Day-2/3 sentence templates with new numbers — real, systemic, and belongs with the context-frame work in the fill rather than a same-day patch. |
| D32 | 2026-07-28 | B1.0 | **The figure renderer shipped as a typed `figure` field plus ten SVG primitives, NOT as a `[figure:{…}]` literal inside the prompt string** (FILL-ARCHITECTURE §2 G1 had sketched the literal). A typed field is checkable by QG-13, cannot be corrupted by prose formatting, and — decisively — lets a picture be added to existing content without disturbing one byte of pack identity. The authored `[image: …]` direction therefore STAYS in `prompt`: it is what QG-1/QG-4 sign for operand freshness (several Level-A items carry their only numeric token inside it), and the renderer strips it from the screen while using its words as the picture's accessible name and as the scene a pre-reader hears. Ten type tags cover the nine specified primitives (`ten-frame` and `counters` are the two halves of "ten-frame / counter groups"). | L29 in practice: separate rendering from identity deliberately. Migrating 22 Level-A prompts and both pinned fixtures cost zero identity churn — the fixtures were verified byte-identical after stripping every `figure` key — so the style-gate calibration and the QG-11 regression set survived a corpus-wide visual change untouched. |
| D33 | 2026-07-28 | B1.0 | **QG-13 audits figures on two axes, and the second one is the point.** (a) Well-formedness: a mark inside its own number line, a shaded count that fits its grid, a triangle whose angles sum to 180°. (b) Contradiction: a figure may declare `asserts:{of,equals}`, and the validator recomputes what the picture claims from the figure's own params and compares it with the item's `answer.value` or a named `generator.params` key — the `answerFor` discipline applied to pictures. An unrecognised selector returns null and FAILS rather than silently auditing the default quantity (the figure twin of an unregistered templateId skipping QG-5). Comparison is generous about punctuation and units (a picture carries no units, so "40" matches "40 m") and strict about value. | Written down explicitly, per L30: what QG-13 does NOT cover is a figure with no `asserts`, which is checked only for possibility — so a picture showing the wrong CONTEXT (six apples for a story about pears) still passes. That uncovered surface is where the next real bug lives; the style gate reads figures for it. |
| D34 | 2026-07-28 | B1.0 | **Level-A noun pools restricted to nouns that have a glyph** (`DRAWABLE_NOUNS` in `lib/figures.ts`), dropping bears/kites/frogs/pebbles/crayons/acorns from a01/a02. A picture of generic dots standing beside "count the acorns" is a small lie a five-year-old can see, and inventing six more glyphs would grow the schema for every future noun. Where a pinned fixture's noun cannot be changed (its text is frozen), plain counters are drawn and the figure's `alt` says "counters" rather than claiming to be ladybugs. | The alternative — a generic counter for every noun — makes the picture always technically true and never actually about the story. Restricting the pool costs a little variety and buys pictures that mean what the sentence says. Level-A prose that named a dropped noun was corrected in the same pass. |
| D35 | 2026-07-28 | B1.0 | **Eight Level-A surfaces are DECLARED figure debt, not silently tolerated.** Three a02 shape-pattern items + its pattern-train puzzle need a shape-strip primitive; A15's finger-count, shape-gallery and labelled-sum-garden need primitives the ten do not include; and A15-D4-03 (7 red balloons vs 5 blue, "which colour has MORE?") is deliberately NOT drawn — its own unchangeable distractor rationale says one group is drawn larger and spread out, and a one-to-one compare figure would hand over the very answer the conservation trap exists to withhold. `bb-verify-packs` prints the list every run and FAILS on any *undeclared* Level-A placeholder, so the debt cannot grow quietly. | The L27 harm — literal bracket characters reaching a pre-reader — is now structurally impossible everywhere, because `PromptFigure` renders an un-migrated direction as a quiet caption and never raw brackets. What remains is missing artwork, which is a coverage fact and should be visible as one rather than hidden behind a green gate. |
| D36 | 2026-07-28 | B1.0 | **Answer-giving scaffolds in a figure are OPT-IN, and that default is the pedagogy.** The Fable review found the compare layout always drawing one-to-one threads and ringing the leftovers, and the hiding game giving each covered counter its own countable cover. Both are correct pictures and both solve the item. Added `showPairs` / `markExtra` (default false) and `coverStyle:'single'`; re-authored d04's independent guided example to show only the given bar. The rule now stated in the schema doc comments: turn a scaffold on to MODEL a strategy in a lesson script or worked example, leave it off on anything that assesses. | This is the visual sibling of L25 ("who does the cognitive work in this sentence?"), and it is invisible to every deterministic gate — QG-13 confirms a picture is TRUE, never that it is fair. A figure that is accurate and helpful can still delete the assessment, and the default has to be the safe one because the fill will attach hundreds of these. Logged as L33. |

