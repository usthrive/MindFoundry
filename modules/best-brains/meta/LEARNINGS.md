# LEARNINGS.md — feeds the Phase 7 meta-prompt v2

Everything that would make this pipeline better next time. Captured continuously, not retroactively. Newest at bottom.

| # | Phase | Learning | v2 implication |
|---|-------|----------|----------------|
| L1 | 0 | The v1 prompt says "repo root" but the working directory contained the repo one level down (`Math_Tutor/MindFoundry/`), forcing a judgment call (D1). | v2 should take an explicit `{{MODULE_ROOT}}` parameter or define "repo root = git root". |
| L2 | 0 | The orchestrator had already surveyed the codebase before receiving the meta-prompt (user asked for an understanding pass first). This saved ~15 min. | v2 could make "Phase 0: codebase survey" an explicit cheap phase with a written artifact, so cloned runs don't depend on prior conversation context. |
| L3 | 1 | Highest-yield sources, in order: (1) app-store screenshots + version history (revealed grading taxonomy, Level→Week addressing, test flow), (2) official sample worksheet images on the marketing CDN, (3) teacher job ads + employee reviews (revealed role split and real class composition). Generic review sites were low-yield and often 403'd. | v2 Phase 1 should rank source categories by expected yield and say explicitly: "mine app screenshots and job ads hard; expect Yelp/aggregators to be blocked". |
| L4 | 1 | A same-name foreign brand ("Best Brain" Ghana) polluted search results and could have poisoned the ledger. | v2 needs a `{{DISAMBIGUATION}}` parameter: known confusable names to exclude, checked at Phase 1 launch. |
| L5 | 1 | Parent-voice research found near-zero organic forum discourse for this program — a possibility v1 didn't anticipate; the agent handled it by treating absence as a finding. | v2 should state: "absence of a source category is itself a finding; record it and redistribute effort to the categories that pay." |
| L6 | 1 | Labeling-at-capture (D4) worked: all six agents returned pre-labeled findings, making the Phase 2 audit cheap. | Keep in v2 as a hard rule. |
| L7 | 3 | **Wayback Machine was the single highest-yield Phase 3 vector** — a 2013 answer-key site + archived /math tabs recovered the entire historical level ladder that current sources hide. | v2 Phase 1 should include "web.archive.org sweep of the official domain's historical pages" as a FIRST-CLASS source category, not a Phase 3 afterthought. |
| L8 | 3 | YouTube transcripts are hard-blocked from this environment (7 retrieval routes tried, all gated). Video value came from metadata, descriptions, and thumbnail frames instead. | v2 should say: don't budget for transcript mining; treat video as metadata+frames only, and put interview transcripts on the user-assisted list (external inbox request). |
| L9 | 3 | Marketing copy contradicts itself usefully: "non-repetitive / never review" vs "weekly repetition with increasing complexity" both appear in official articles. Contradiction-hunting inside the official corpus resolved the spiral question without any third-party source. | v2 Phase 3 should include "search the official corpus for statements that contradict the tagline" as a named technique. |
| L10 | 3 | Gap→attack-vector tables (each gap with 3–5 named, specific search moves) made the targeted agents dramatically more effective than open-ended "close this gap" prompts. | v2 should require the GAP-LIST to carry attack vectors per gap, written at Phase 2 time. |
| L11 | 4 | A design sub-agent died to a transient API error AFTER reading all inputs but BEFORE writing anything — total work loss was avoided only because resume-with-context existed. | v2 should instruct all sub-agents producing multiple artifacts: write each artifact to disk the moment it is complete; never batch writes to the end. |
| L12 | 4 | The user's external research drop arrived in a different folder than the designated inbox (and mid-phase, as predicted). Reconciliation was cheap because the report used the same Confirmed/Inferred/Speculative labeling — Appendix A's prompt design paid off. | v2: keep Appendix A's label-mandate; add "watch for drops OUTSIDE the inbox folder too"; formalize post-freeze ledger revisions as numbered blocks (P3.1, P3.2…) rather than reopening the freeze. |
| L13 | 4 | Cross-model corroboration is a real confidence signal: an independent frontier-model report reproduced our reconstruction (weekly cycle, grader split, level opacity) with zero material contradictions. | v2 could add an explicit "cross-model agreement" confidence upgrade rule to the evidence protocol. |
| L14 | 6 | The design round-trip's file-return leg was the weak link: the share link was auth-walled (WebFetch 403), the browser bridge was down, and the working tool (DesignSync) is main-conversation-only — four sub-agent transfer attempts failed before this was discovered, and a full-fidelity transfer of 38 files through the main context was unaffordable (solved with a priority-subset + grammar-generalization strategy). | v2 §8.1 should say: specify the return path CONCRETELY (repo drop preferred over share link), test the retrieval tool's sub-agent availability BEFORE fanning out transfer agents, and pre-authorize a "priority subset + generalize" fallback for large design exports. |
| L15 | 6 | A host-process restart killed a builder mid-increment; resume-from-transcript + write-as-you-go (L11) meant zero work lost, but the orchestrator had to manually verify disk state before resuming. | v2 should include a standard resume drill: on any interruption notification, first `git status` + artifact listing, then resume the agent with a state summary — never relaunch blind. |
| L16 | 7 | A persona-runner agent launched its Playwright drive as a background process and then repeatedly ended its turn "waiting" — three resumes couldn't unstick it (it even re-ran the drive redundantly). The run artifacts were fine; the analysis had to be handed to a fresh agent working from disk. | v2 testing phase should instruct runners: execute browser drives SYNCHRONOUSLY (foreground Bash), never as background processes they then wait on; and the orchestrator's rule is "two failed resumes → stop resuming, spawn a fresh analyst on the on-disk artifacts" (run-artifact/analysis separation makes this cheap). |
| L19 | 6/engine | **Correctness ≠ authenticity — proven, not asserted.** The generated Level-D content passed the correctness validator cleanly (2857 assertions, 0 failures) but FAILED the authenticity gate 1/24. A deterministic/structural gate cannot see pedagogical hollowness; it happily passes "single-step noun-swap in a 5-day-arc costume". The two-gate architecture (correctness AND authenticity) is not belt-and-suspenders — each catches a class the other is blind to. | v2 engine phase: BOTH gates are MANDATORY and the FIRST content batch must clear the authenticity gate at a threshold (≥80% clean-ACCEPT) BEFORE any scale-up. Never authorize bulk generation off a correctness-only proof. Encode "correctness-gate-green is necessary but NOT sufficient" as a hard rule. |
| L20 | 6/engine | **A single-pass LLM authenticity judge is fooled by presentation polish.** The base scorer passed 12/24; the adversarial anti-drift re-check (attack the 3 sharpest gates, default to overturn) reversed 11 of those 12. Without the adversarial layer the gate would have reported ~50% authentic — wildly wrong. | v2: authenticity/quality judging REQUIRES an adversarial verify layer (attack-and-overturn), never a single judge. This mirrors the persona-review adversarial pattern; make it standard for any LLM-as-judge gate. Also: a correctness validator that audits only item *keys* has coverage holes — audit narrative/anchor numbers and distractor-vs-key inequality too (bugs surfaced in W6/W8/W9). |
| L24 | Post-rebuild audit | **Both gates measure pedagogical DEPTH within one unit; neither measures SURFACE REALISM, and neither looks ACROSS units.** A fresh-model audit of the 100%-ACCEPT Level-D corpus found four defect classes that ship despite both gates passing: money rendered at one decimal (`"$0.5"`, and `"added $0.7 and $0.40"` in one prompt); unreduced fractions in real-world prose (`"2/4 cup of flour"`); the same real-world context converging across different weeks of one archetype (seats for every multiplication week); duplicate warm-up formats in a day. The arithmetic audit passes all of these because the *values* are right — only presentation and cross-unit variety are wrong. A human tester spotted #3 within minutes of first viewing the content. | Add a **surface-realism layer** to the engine spec, enforced the same way depth is (library fix + preflight gate, so regression is impossible): a units/currency rendering rule; a **context-sensitive** simplification rule (never blanket — unreduced fractions are pedagogically CORRECT in equivalence/comparison tasks); a **cross-unit context ledger** so consecutive same-archetype units rotate contexts. Also: schedule a **fresh-eyes audit by a different/newer model** after any corpus passes its gates — the gate authors and the content authors share blind spots, and a human will find these instantly. |
| L23 | Step-2 rebuild | **Exemplar-first + adversarial-spec-review is the way to scale content generation.** Sequence that worked: (a) write the build spec, then run a 4-lens adversarial review of it BEFORE any code (caught 7 blockers incl. a mechanism that physically couldn't run) → (b) build the library + prove ONE exemplar week (D4) through BOTH gates → (c) distill the pattern into a per-week authoring kit → (d) run a small validation WAVE (4 weeks) to prove the kit/process (caught the D10 `initD` bug before it propagated) → (e) fan out the rest. | v2 framework: for any generation-at-scale phase, mandate spec-review-before-build + exemplar-before-fanout + a validation-wave gate. Never fan out N agents on an unproven kit. |
| L22 | Step-2 rebuild | **The adversarial style-gate stage catches defects BOTH deterministic gates and the base scorer miss.** It brute-forced an authored cryptarithm puzzle and proved it *unsolvable*; the puzzle's answer was `manual-review`, so no arithmetic audit applied, and the base scorer was fooled by polished framing. | Keep the adversarial anti-drift re-check as a REQUIRED stage — it is not redundant with the scorer. Any authored puzzle with a definite answer should carry a checkable (non-`manual-review`) key where possible. |
| L21 | Step-2 rebuild | **Code-derive embedded-claim truth, mirroring the code-computed-answer rule.** A `verifyFor(params)` registry recomputes the CORRECT answer of a discrimination / error-analysis item from its serializable `generator.params` (exactly as `answerFor` serves QG-5). The error-analysis "wrong" value is a NAMED misconception transform applied by code, never authored. | This makes the D6 (keyed-a-wrong-answer) and D8 (fabricated-anchor) bug classes structurally impossible for generated items; the v2 QG-11 detector additionally FORBIDS hand-authored embedded-claim items. Reuse for every future program. |
| L20 | Step-2 rebuild | **The correctness gate and the authenticity gate are genuinely complementary — correctness ≠ prose-consistency.** QG-5 recomputes the answer from `generator.params`; a multi-step fraction chain that forgot `initD` computes an answer consistent with its (wrong) params, so correctness PASSES while the prompt prose contradicts it. Only the LLM style gate, which READS the prose, caught it (D10). | Do not treat a correctness-green pack as authentic or even self-consistent. The two-gate architecture is load-bearing; keep both. A syntactic prose↔computation guard is not feasible (first fraction is often an operand, e.g. "3/8 of 48") — the reading gate is the check. |
| L19 | Step-2 rebuild | **Pedagogical preflight runs at PACK-GENERATION time, so every gate must be SEED-INVARIANT.** Packs regenerate per learner `pack_seed`; a gate that depends on drawn operands (the hint-dedup, keyed on templated hints) is seed-variant and throws for seeds the CI never tested → a broken pack for that learner. Fix: fixed, role-based, NAME/NUMBER-FREE hints + a digit-normalized dedup, and reuse each generator ≤2× in core. | Rule for all generator gates: assert on blueprint STRUCTURE, not on per-seed surfaces; if a check must touch operands, normalize so it throws for all seeds or none. Sweep ≥200 seeds when authoring a week, not just the CI 5. |
| L18 | 7 (framework) | **Model tier and verification are substitutes.** Where work is gated by a deterministic validator + an adversarial style gate, the *generator's* model tier barely affects correctness — the gate is the bottleneck, so a strong-but-not-top model (Opus 4.8) is fine for gated execution. Where work resists gating — pedagogical *ceiling* (does the explanation truly teach the why; is the word problem genuinely engaging), and completeness ("what did we miss," gaps between artifacts, unstated assumptions) — the top model's marginal value is highest. User proposal (adopted): use the top model (Fable 5) **strategically as an adversarial review/completeness layer, not the workhorse** — cheaper AND higher-quality than using it throughout. | **v2 framework: add a "Strategic Top-Model Review" layer** invoked at defined checkpoints, NOT as the default executor: (1) after the Question-Engine phase — a top-model pass over a SAMPLE of generated content + the style-gate results, hunting authenticity/pedagogy gaps the LLM-judge missed; (2) a final whole-build **completeness critic** ("what modality/claim/artifact-gap did we miss across everything"); (3) the meta-prompt self-update itself. Rule of thumb to encode: spend the top model where judgment compounds and gating can't reach (pedagogy ceiling, completeness, novel synthesis); use a strong workhorse under deterministic+adversarial gates for volume execution. This is the same independent-perspective-verify / completeness-critic pattern the pipeline already uses, with the strongest model reserved for the highest-leverage pass. |
| L17 | 6 | **The question-creation engine is THE core capability of an education product, and v1 undersold it** — folded into a Phase-4 spec + a Phase-6 "seed 6 of 120 weeks" sample. It surfaced late (post-Phase-7) that only 9/120 cells had content, and that the engine's real design questions (deterministic-vs-AI generation, the answer-correctness guarantee, AI-assisted authoring at scale, full-coverage completeness) were never a first-class phase. **User directive: make the question engine an explicit phase and document it as a reusable capability for future systems builds.** | **v2 MUST add a dedicated phase — "Phase 4.5 / ENGINE: Question-Generation Capability"** — between teaching-system design and build. It must cover: (1) generation architecture (deterministic templates + seeded RNG vs live-AI, and why deterministic wins for cost/correctness); (2) the **answer-correctness invariant** — answers COMPUTED by code (`answerFor`), never asserted by an AI, and validated by quality gates + an arithmetic audit; (3) AI's proper role — question *structure* + *language* (explanations, hints, word-problem phrasing, puzzles), not answers; (4) an **AI-assisted batch-authoring pipeline** (one-time, offline, gate-validated) to reach FULL coverage, not a token seed; (5) an explicit coverage ledger (every level×unit either generated+validated or flagged for review). The build phase then consumes a proven engine instead of hand-seeding a sample. Document the engine as a portable capability spec so cloned program-modules inherit it. |
