# LEARNINGS.md — feeds the Phase 7 meta-prompt v2

Everything that would make this pipeline better next time. Captured continuously, not retroactively. Newest at bottom.

| # | Phase | Learning | v2 implication |
|---|-------|----------|----------------|
| L1 | 0 | The v1 prompt says "repo root" but the working directory contained the repo one level down (`Math_Tutor/MindFoundry/`), forcing a judgment call (D1). | v2 should take an explicit `{{MODULE_ROOT}}` parameter or define "repo root = git root". |
| L2 | 0 | The orchestrator had already surveyed the codebase before receiving the meta-prompt (user asked for an understanding pass first). This saved ~15 min. | v2 could make "Phase 0: codebase survey" an explicit cheap phase with a written artifact, so cloned runs don't depend on prior conversation context. |
| L3 | 1 | Highest-yield sources, in order: (1) app-store screenshots + version history (revealed grading taxonomy, Level→Week addressing, test flow), (2) official sample worksheet images on the marketing CDN, (3) teacher job ads + employee reviews (revealed role split and real class composition). Generic review sites were low-yield and often 403'd. | v2 Phase 1 should rank source categories by expected yield and say explicitly: "mine app screenshots and job ads hard; expect Yelp/aggregators to be blocked". |
| L4 | 1 | A same-name foreign brand ("Best Brain" Ghana) polluted search results and could have poisoned the ledger. | v2 needs a `{{DISAMBIGUATION}}` parameter: known confusable names to exclude, checked at Phase 1 launch. |
| L5 | 1 | Parent-voice research found near-zero organic forum discourse for this program — a possibility v1 didn't anticipate; the agent handled it by treating absence as a finding. | v2 should state: "absence of a source category is itself a finding; record it and redistribute effort to the categories that pay." |
| L6 | 1 | Labeling-at-capture (D4) worked: all six agents returned pre-labeled findings, making the Phase 2 audit cheap. | Keep in v2 as a hard rule. |
| L7 | 3 | **Wayback Machine was the single highest-yield Phase 3 vector** — a 2013 answer-key site + archived /math tabs recovered the entire historical level ladder that current sources hide. | v2 Phase 1 should include "web.archive.org sweep of the official domain's historical pages" as a FIRST-CLASS source category, not a Phase 3 afterthought. |
| L8 | 3 | YouTube transcripts are hard-blocked from this environment (7 retrieval routes tried, all gated). Video value came from metadata, descriptions, and thumbnail frames instead. | v2 should say: don't budget for transcript mining; treat video as metadata+frames only, and put interview transcripts on the user-assisted list (external inbox request). |
| L9 | 3 | Marketing copy contradicts itself usefully: "non-repetitive / never review" vs "weekly repetition with increasing complexity" both appear in official articles. Contradiction-hunting inside the official corpus resolved the spiral question without any third-party source. | v2 Phase 3 should include "search the official corpus for statements that contradict the tagline" as a named technique. |
| L10 | 3 | Gap→attack-vector tables (each gap with 3–5 named, specific search moves) made the targeted agents dramatically more effective than open-ended "close this gap" prompts. | v2 should require the GAP-LIST to carry attack vectors per gap, written at Phase 2 time. |
| L11 | 4 | A design sub-agent died to a transient API error AFTER reading all inputs but BEFORE writing anything — total work loss was avoided only because resume-with-context existed. | v2 should instruct all sub-agents producing multiple artifacts: write each artifact to disk the moment it is complete; never batch writes to the end. |
| L12 | 4 | The user's external research drop arrived in a different folder than the designated inbox (and mid-phase, as predicted). Reconciliation was cheap because the report used the same Confirmed/Inferred/Speculative labeling — Appendix A's prompt design paid off. | v2: keep Appendix A's label-mandate; add "watch for drops OUTSIDE the inbox folder too"; formalize post-freeze ledger revisions as numbered blocks (P3.1, P3.2…) rather than reopening the freeze. |
| L13 | 4 | Cross-model corroboration is a real confidence signal: an independent frontier-model report reproduced our reconstruction (weekly cycle, grader split, level opacity) with zero material contradictions. | v2 could add an explicit "cross-model agreement" confidence upgrade rule to the evidence protocol. |
| L14 | 6 | The design round-trip's file-return leg was the weak link: the share link was auth-walled (WebFetch 403), the browser bridge was down, and the working tool (DesignSync) is main-conversation-only — four sub-agent transfer attempts failed before this was discovered, and a full-fidelity transfer of 38 files through the main context was unaffordable (solved with a priority-subset + grammar-generalization strategy). | v2 §8.1 should say: specify the return path CONCRETELY (repo drop preferred over share link), test the retrieval tool's sub-agent availability BEFORE fanning out transfer agents, and pre-authorize a "priority subset + generalize" fallback for large design exports. |
| L15 | 6 | A host-process restart killed a builder mid-increment; resume-from-transcript + write-as-you-go (L11) meant zero work lost, but the orchestrator had to manually verify disk state before resuming. | v2 should include a standard resume drill: on any interruption notification, first `git status` + artifact listing, then resume the agent with a state summary — never relaunch blind. |
| L16 | 7 | A persona-runner agent launched its Playwright drive as a background process and then repeatedly ended its turn "waiting" — three resumes couldn't unstick it (it even re-ran the drive redundantly). The run artifacts were fine; the analysis had to be handed to a fresh agent working from disk. | v2 testing phase should instruct runners: execute browser drives SYNCHRONOUSLY (foreground Bash), never as background processes they then wait on; and the orchestrator's rule is "two failed resumes → stop resuming, spawn a fresh analyst on the on-disk artifacts" (run-artifact/analysis separation makes this cheap). |
| L19 | 6/engine | **Correctness ≠ authenticity — proven, not asserted.** The generated Level-D content passed the correctness validator cleanly (2857 assertions, 0 failures) but FAILED the authenticity gate 1/24. A deterministic/structural gate cannot see pedagogical hollowness; it happily passes "single-step noun-swap in a 5-day-arc costume". The two-gate architecture (correctness AND authenticity) is not belt-and-suspenders — each catches a class the other is blind to. | v2 engine phase: BOTH gates are MANDATORY and the FIRST content batch must clear the authenticity gate at a threshold (≥80% clean-ACCEPT) BEFORE any scale-up. Never authorize bulk generation off a correctness-only proof. Encode "correctness-gate-green is necessary but NOT sufficient" as a hard rule. |
| L20 | 6/engine | **A single-pass LLM authenticity judge is fooled by presentation polish.** The base scorer passed 12/24; the adversarial anti-drift re-check (attack the 3 sharpest gates, default to overturn) reversed 11 of those 12. Without the adversarial layer the gate would have reported ~50% authentic — wildly wrong. | v2: authenticity/quality judging REQUIRES an adversarial verify layer (attack-and-overturn), never a single judge. This mirrors the persona-review adversarial pattern; make it standard for any LLM-as-judge gate. Also: a correctness validator that audits only item *keys* has coverage holes — audit narrative/anchor numbers and distractor-vs-key inequality too (bugs surfaced in W6/W8/W9). |
| L24 | Post-rebuild audit | **Both gates measure pedagogical DEPTH within one unit; neither measures SURFACE REALISM, and neither looks ACROSS units.** A fresh-model audit of the 100%-ACCEPT Level-D corpus found four defect classes that ship despite both gates passing: money rendered at one decimal (`"$0.5"`, and `"added $0.7 and $0.40"` in one prompt); unreduced fractions in real-world prose (`"2/4 cup of flour"`); the same real-world context converging across different weeks of one archetype (seats for every multiplication week); duplicate warm-up formats in a day. The arithmetic audit passes all of these because the *values* are right — only presentation and cross-unit variety are wrong. A human tester spotted #3 within minutes of first viewing the content. | Add a **surface-realism layer** to the engine spec, enforced the same way depth is (library fix + preflight gate, so regression is impossible): a units/currency rendering rule; a **context-sensitive** simplification rule (never blanket — unreduced fractions are pedagogically CORRECT in equivalence/comparison tasks); a **cross-unit context ledger** so consecutive same-archetype units rotate contexts. Also: schedule a **fresh-eyes audit by a different/newer model** after any corpus passes its gates — the gate authors and the content authors share blind spots, and a human will find these instantly. |
| L23 | Step-2 rebuild | **Exemplar-first + adversarial-spec-review is the way to scale content generation.** Sequence that worked: (a) write the build spec, then run a 4-lens adversarial review of it BEFORE any code (caught 7 blockers incl. a mechanism that physically couldn't run) → (b) build the library + prove ONE exemplar week (D4) through BOTH gates → (c) distill the pattern into a per-week authoring kit → (d) run a small validation WAVE (4 weeks) to prove the kit/process (caught the D10 `initD` bug before it propagated) → (e) fan out the rest. | v2 framework: for any generation-at-scale phase, mandate spec-review-before-build + exemplar-before-fanout + a validation-wave gate. Never fan out N agents on an unproven kit. |
| L22 | Step-2 rebuild | **The adversarial style-gate stage catches defects BOTH deterministic gates and the base scorer miss.** It brute-forced an authored cryptarithm puzzle and proved it *unsolvable*; the puzzle's answer was `manual-review`, so no arithmetic audit applied, and the base scorer was fooled by polished framing. | Keep the adversarial anti-drift re-check as a REQUIRED stage — it is not redundant with the scorer. Any authored puzzle with a definite answer should carry a checkable (non-`manual-review`) key where possible. |
| L21 | Step-2 rebuild | **Code-derive embedded-claim truth, mirroring the code-computed-answer rule.** A `verifyFor(params)` registry recomputes the CORRECT answer of a discrimination / error-analysis item from its serializable `generator.params` (exactly as `answerFor` serves QG-5). The error-analysis "wrong" value is a NAMED misconception transform applied by code, never authored. | This makes the D6 (keyed-a-wrong-answer) and D8 (fabricated-anchor) bug classes structurally impossible for generated items; the v2 QG-11 detector additionally FORBIDS hand-authored embedded-claim items. Reuse for every future program. |
| L20 | Step-2 rebuild | **The correctness gate and the authenticity gate are genuinely complementary — correctness ≠ prose-consistency.** QG-5 recomputes the answer from `generator.params`; a multi-step fraction chain that forgot `initD` computes an answer consistent with its (wrong) params, so correctness PASSES while the prompt prose contradicts it. Only the LLM style gate, which READS the prose, caught it (D10). | Do not treat a correctness-green pack as authentic or even self-consistent. The two-gate architecture is load-bearing; keep both. A syntactic prose↔computation guard is not feasible (first fraction is often an operand, e.g. "3/8 of 48") — the reading gate is the check. |
| L19 | Step-2 rebuild | **Pedagogical preflight runs at PACK-GENERATION time, so every gate must be SEED-INVARIANT.** Packs regenerate per learner `pack_seed`; a gate that depends on drawn operands (the hint-dedup, keyed on templated hints) is seed-variant and throws for seeds the CI never tested → a broken pack for that learner. Fix: fixed, role-based, NAME/NUMBER-FREE hints + a digit-normalized dedup, and reuse each generator ≤2× in core. | Rule for all generator gates: assert on blueprint STRUCTURE, not on per-seed surfaces; if a check must touch operands, normalize so it throws for all seeds or none. Sweep ≥200 seeds when authoring a week, not just the CI 5. |
| L18 | 7 (framework) | **Model tier and verification are substitutes.** Where work is gated by a deterministic validator + an adversarial style gate, the *generator's* model tier barely affects correctness — the gate is the bottleneck, so a strong-but-not-top model (Opus 4.8) is fine for gated execution. Where work resists gating — pedagogical *ceiling* (does the explanation truly teach the why; is the word problem genuinely engaging), and completeness ("what did we miss," gaps between artifacts, unstated assumptions) — the top model's marginal value is highest. User proposal (adopted): use the top model (Fable 5) **strategically as an adversarial review/completeness layer, not the workhorse** — cheaper AND higher-quality than using it throughout. | **v2 framework: add a "Strategic Top-Model Review" layer** invoked at defined checkpoints, NOT as the default executor: (1) after the Question-Engine phase — a top-model pass over a SAMPLE of generated content + the style-gate results, hunting authenticity/pedagogy gaps the LLM-judge missed; (2) a final whole-build **completeness critic** ("what modality/claim/artifact-gap did we miss across everything"); (3) the meta-prompt self-update itself. Rule of thumb to encode: spend the top model where judgment compounds and gating can't reach (pedagogy ceiling, completeness, novel synthesis); use a strong workhorse under deterministic+adversarial gates for volume execution. This is the same independent-perspective-verify / completeness-critic pattern the pipeline already uses, with the strongest model reserved for the highest-leverage pass. |
| L17 | 6 | **The question-creation engine is THE core capability of an education product, and v1 undersold it** — folded into a Phase-4 spec + a Phase-6 "seed 6 of 120 weeks" sample. It surfaced late (post-Phase-7) that only 9/120 cells had content, and that the engine's real design questions (deterministic-vs-AI generation, the answer-correctness guarantee, AI-assisted authoring at scale, full-coverage completeness) were never a first-class phase. **User directive: make the question engine an explicit phase and document it as a reusable capability for future systems builds.** | **v2 MUST add a dedicated phase — "Phase 4.5 / ENGINE: Question-Generation Capability"** — between teaching-system design and build. It must cover: (1) generation architecture (deterministic templates + seeded RNG vs live-AI, and why deterministic wins for cost/correctness); (2) the **answer-correctness invariant** — answers COMPUTED by code (`answerFor`), never asserted by an AI, and validated by quality gates + an arithmetic audit; (3) AI's proper role — question *structure* + *language* (explanations, hints, word-problem phrasing, puzzles), not answers; (4) an **AI-assisted batch-authoring pipeline** (one-time, offline, gate-validated) to reach FULL coverage, not a token seed; (5) an explicit coverage ledger (every level×unit either generated+validated or flagged for review). The build phase then consumes a proven engine instead of hand-seeding a sample. Document the engine as a portable capability spec so cloned program-modules inherit it. |
| L25 | Phase A review | **A presence-gate can be satisfied by the teacher's voice doing the child's job.** The metacog gate (§6.8) requires an estimate-first item + a modeled script segment — both present in all 23 ACCEPT weeks — yet the wrapper implements it as a declarative prefix that states the estimate AND its structural reason ("rounding up always adds one more whole trip…"), sometimes pre-naming the exact move the item exists to make the child choose (D7). The gate measured that metacognition APPEARS, not WHO PERFORMS it. Same pattern in two error-analysis prompts that name the error they ask the child to find (D21-D5-01). | For any pedagogy gate, ask "who does the cognitive work in this sentence?" — encode ELICIT-not-NARRATE as a structural rule where possible (prefix must be interrogative, must not contain the answer-move's name; EA prompts from claim-only templates), and make it an explicit checkpoint question in every future top-model review. |
| L26 | Phase A review | **Surface-realism bugs cluster where an engineering convenience overrides the week's own content law.** "Comma-free surfaces for clean tokenization" is invisible in most weeks but breaks the one week whose CONCEPT is reading big numbers in groups of three (D1 prints 433606 while its explanation teaches 507,036); money-as-decimal is fine in the abstract but wrong the moment the situation renders it as a price; independent noun/predicate draws are fine until a frame implies fruit ("ripe marbles"). | When auditing generated content, deliberately read the weeks whose concept IS the surface convention (place value → number formatting; money weeks → currency; fraction weeks → reduction) — that's where presentation bugs are content bugs. And when a template interpolates a quantity, the interpolation must go through a semantic formatter (format.ts), never raw `${…}`. |
| L27 | Phase B0 | **A "visual-first" curriculum can be entirely text, and every gate can still pass.** Measured on the shipped Level-D corpus: 76/76 lesson-script segments carry a well-written `visual` direction that renders as *italic prose in a dashed box* (the code comment says a design pass will draw it; it never ran); 0/96 guided examples have a visual field at all (the schema has no slot); 0/610 day items carry a figure; and Level A has 18 `[image: …]` placeholders that the practice screen prints as literal characters to a pre-reader. Meanwhile BB-W6 (visual→symbolic band match) and BB-W8 (answer mode matches task) were being scored by an LLM reading those stage directions — grading the description of a picture nobody rendered. | Same shape as L24/L25, one layer up: **the gate cannot see what the child sees.** For any future program, (a) assert that each schema field with pedagogical weight has a RENDERER, not just a value — a field whose consumer is a placeholder is not implemented; (b) never let a judge score a modality it is reading a description of; (c) when a curriculum's pedagogy is "concrete model first", the figure primitives are a Phase-BUILD blocker, not polish. Generate figures from the item's OWN computed params so a picture cannot contradict its answer — the `answerFor` discipline applied to pictures. |
| L28 | Phase B0 | **A formatter is a guarantee; a scan is only a backstop — and you need both, in that order.** The B0 pass fixed seven surface-defect classes twice over: once in `lib/format.ts` (templates that can only interpolate through `money()/countNoun()/fmtInt()` cannot emit a violation at any seed) and once as QG-12a–d scans over every emitted surface. The scans then earned their keep immediately by finding what the library pass had missed — because the library only governs templates that CALL it, and the scans read everything: a broken singular inside a distractor RATIONALE ("another boxe still fills"), "1 hundreds" in two old-engine C weeks, "1 kites" in Level A, and — the sharpest one — a defect in prose written EARLIER IN THE SAME PASS by the same author who was building the guard. | Sequence for any content-quality property: (1) put the rule in a library function the templates must call, (2) add a scan over the emitted artefact that proves no template bypassed it, (3) write regression fixtures that prove the scan FIRES and that its false-positive guards do NOT. A scan without step 3 is untested; a library without step 2 silently degrades the moment someone hand-writes a string. And never assume the author of the guard is exempt from it. |
| L29 | Phase B0 | **Ordering matters when a rendering change touches identity-bearing machinery.** Turning on thousands separators looked like a one-line flag, but the corpus's freshness (QG-1), isomorph-disjointness (QG-4), arithmetic audit (QG-5) and anchor audit (QG-11) all key off numeric TOKENS parsed from prose. Flipping the flag first would have fractured every signature in the corpus at once. Landing the four prerequisites in order — input parser already comma-stripping, `surface.ts` matching a grouped literal as ONE normalised token, `validator.numericValue` separator-tolerant, QG-11's embedded-value check separator-tolerant — made the flip a no-op for identity and a pure surface change. The QG-11 tolerance was only discovered as necessary because a money-rendered error-analysis value ("$11,198") stopped matching its canonical form ("11198"). | Before enabling any change to how a VALUE is rendered, enumerate every consumer that parses that rendering for identity or audit purposes and make each one normalise first. Rendering and identity must be separated deliberately: keep `answer.value` and `generator.params` canonical, let the UI and prose format, and make every audit compare on the normalised form. |
| L30 | B0.8 style gate | **A judge that RECOMPUTES finds a class a judge that reads cannot — and it found semantically wrong content that had passed 7,877 deterministic assertions AND a 100% authenticity run.** Instructing the adversarial judges to recompute every distractor and every stated answer by hand surfaced two genuine answer-key errors: D7's Day-5 exemplar taught that 45÷6 "rounds up to 7" (it rounds up to 8; 7 is the drop answer — inverting the exact distinction that week exists to teach), and D8's puzzle key named the wrong missing room of 26×34 while asserting 6×30=80, with a mistakeBank total of 674 where the arithmetic gives 642. Both sat in HAND-AUTHORED prose inside a `manual-review` answer and a mistakeBank entry — precisely the surfaces QG-5 does not audit, because it re-derives *computed* answers from `generator.params`. The same pass also found a whole generator class where a distractor's named misconception would have selected the CORRECT answer. | Two rules. (1) Any LLM-as-judge stage must be told to RECOMPUTE rather than verify-by-reading; "check the arithmetic" and "recompute the arithmetic yourself" are different instructions and only the second finds this class. (2) Wherever a gate's coverage stops (here: prose inside manual-review answers, mistakeBank exemplars, and puzzle keys), write down explicitly what is NOT covered — the uncovered surface is where the next real bug lives. The v2 rebuild made *generated* embedded claims impossible via `verifyFor`; it did not make *authored* exemplar prose safe, and nothing in the pipeline was watching that gap. |
| L31 | B0.8 style gate | **A "puzzle" can pass remove-the-concept against Day 1 while being a verbatim clone of the week's own Day-2/3/4 template.** BB-G7 tests the puzzle against Day-1 core items, so two packs shipped puzzles that were exactly their week's daily multi-step template re-skinned with a Puzzle Grove title (D11: k copies + a whole; D16: the bracketing method rehearsed in `script[2]` and again in D5-02). Both escaped on the letter of the rule while adding no search, constraint, deduction or justification. Only an adversarial judge comparing the puzzle against the WHOLE week caught it. | **Tried to encode this as a preflight over all core days, and REVERTED it** — the attempt is the lesson. 18 of 23 puzzles declare the generic `cognitiveOp: 'multi-step'`, which the daily core also produces, so the widened gate fired on 14 weeks whose puzzles the judges had assessed as genuinely distinct: it was measuring the coarseness of the LABEL, not the structure. **A gate can only be as sharp as the vocabulary it clusters on.** Making this checkable first requires a puzzle-OPERATION taxonomy (each puzzle declaring the new move it demands — search / constraint / repair / completeness-argument / deduction), which is authoring work, scheduled with the fill; until then the whole-week judgment stays with the style gate, which demonstrably catches it. Replacement puzzles should be searches, constraint problems, repairs or completeness arguments — structures a daily drill cannot accidentally become. |
| L32 | B1.0 | **A renderer needs a render test, because "the field has a value" is exactly the illusion that hid L27.** QG-13 audits figure DATA and was green while every labelled triangle drew as an equilateral one: `resolveAngles` treated "nothing missing" as a failure to solve and fell back to the regular shape, so a triangle labelled 40/60/80 was drawn 60/60/60 — in the week whose discrimination is *classify by the LARGEST angle*. The params were honest, the validator had nothing to object to, and only rendering the SVG and recomputing the drawn interior angles from the emitted polygon caught it. The same suite also caught a counters layout where `arrangement:'rows'` drew a single row of six beside a prompt reading "in two rows". | For any generated artefact with a VISUAL consumer, add a third gate beside the data validator: render it and measure the render. Assert what the medium must satisfy — purity (same params → identical output), no NaN in a coordinate, the accessible name survives, no forbidden colour reaches a child surface — and, where the picture encodes a quantity, recompute that quantity FROM THE RENDERED OUTPUT and compare it with the label. A data gate cannot see a drawing that disagrees with its own parameters. |
| L33 | B1.0 | **The most dangerous figure is not a wrong one, it is a helpful one.** Two surfaces were deliberately left undrawn because the correct picture would destroy the item: A15's "7 red balloons vs 5 blue — which colour has MORE?" (a one-to-one compare figure rings the leftovers and hands over the answer the conservation trap exists to withhold), and every ten-frame that shows the count-on START rather than the total (asserting on it would make the picture claim 7 = 9). Both were found by asking, of each figure, *what does this let the child skip?* | Add to any figure/illustration spec an explicit test alongside "is it correct": **does it give away the answer the item is asking for, or short-circuit the strategy the week exists to teach?** A picture is a scaffold, and a scaffold placed under the exact step being assessed removes the assessment. This is the visual sibling of L25 ("who does the cognitive work in this sentence?"). |
| L34 | B1/B3 fill | **A recipe can specify a misconception the verification library cannot express — and the pressure that creates is to fabricate.** `FILL-ARCHITECTURE`'s per-week rows name an error-analysis misconception for all 96 cells, but the `verifyFor` library varies the OPERATION over a fixed operand pair, or the fraction/decimal move; it has no transform for a two-step step-order slip, a distributive double-count (2ab), or "reports the denominator". Three of the first five Level-C weeks hit this independently. Each author reached the same honest conclusion — one proved algebraically that no story-native operand pair yields both the correct value and that particular wrong one — and reframed rather than inventing a number: posing the COMPLEMENT question so the shown error is a genuine division output, or relocating the misconception to a discrimination option where it needs no `wrong` value at all. | When a spec enumerates content requirements (misconceptions, edge cases, variants) INDEPENDENTLY of the machinery that must verify them, audit the two against each other before fan-out, not during it. Every author who hits the gap pays the discovery cost again, and the tempting resolution — hand-write the wrong number — is exactly the D6/D8 bug class the `verifyFor` registry was built to make impossible. Either extend the library first, or write the sanctioned workaround into the kit BEFORE the fan-out so the pressure never lands on an individual author. |
| L35 | B3 fill | **Every gate judged ONE pack; the defect that scales judges the corpus.** An author reported catching themselves shipping hint ladders copied word-for-word from another week — "both gates passed before I found this; the 200-seed check cannot see it." They are right, and structurally so: the pedagogical preflight dedups ladders INSIDE a week and QG-1/QG-4 keep operand surfaces fresh INSIDE a week, but a fill authors 88 weeks by handing every author the same two proven exemplars to copy. Voice plagiarism is the predictable consequence and no per-pack gate can see it. Built `bb-cross-week-test.ts` to read the whole corpus at once: shared hint ladders, shared prompt SHAPES (digits and punctuation stripped, so "same sentence, new numbers" surfaces), and context convergence by noun. Measured on 41 packs: exactly ONE shared ladder, between the two old-engine Level-A weeks that share generators by design — the fan-out is clean, because the authors were told to read their own week and did. | When work is fanned out by copying an exemplar, add a gate at the CORPUS level before the fan-out, not after: the per-unit gates are structurally blind to imitation, and imitation is exactly what exemplar-first encourages. Note also which half of this was catchable — ladder/shape reuse is mechanical, but "is the answer given away in the prompt" turned out NOT to be (the mode of a list is always in the list; the partner of 5 in 10 is 5), so that one ships as a report-only census a human reads. Do not force a noisy check into a blocking one; a gate that cries wolf gets switched off, and this codebase already learned that once. |
| L36 | B3 fill | **"Not derivable" was sometimes a failure of search, not a fact.** Seven weeks in a row reported that `FILL-ARCHITECTURE`'s named error-analysis misconception could not be produced by any registered `verifyFor`, and reframed around it (correctly — never fabricating). Then C4 was told the borrow-across-zero slip was underivable and found the identity that produces it: for a round-hundred minuend, writing every column smaller-from-larger yields exactly `a + b`, so an operation-swap template generates the recipe's own misconception with nothing invented. The workaround protocol was sound and had become a reflex. | Order matters in a documented escape hatch. When you write "if X is impossible, do Y", authors reach for Y — so the instruction must first say **prove X is impossible**. The kit now reads: look for the identity FIRST, reframe second. Related: C4 also found that a computable answer is not an askable question ("how much trading does 601−462 need?" has two defensible answers), a class no gate can see because the gates check key-vs-params, never whether the prompt admits one reading. |
| L37 | B3 fill | **A gate that checks one form of a rule usually checks only that form.** QG-12c was built in B0 to catch article disagreement and had a numeral arm only — "a 8 cm strip" was caught, "a obstacle course" was not. An author found the word case by reading their own week; extending the scan then immediately found **"A archive has 351 shelves" in D15**, i.e. in the CERTIFIED Level-D corpus, past 7,877 assertions, a 23/23 authenticity pass and every gate since. The fix had to be case-AWARE, not case-insensitive: a lowercase "a" is an article anywhere, but a capital "A" is one only at a sentence start — mid-sentence it is a label ("Class A and Class B"), which the naive version flagged as an error. English keys the article to the SOUND, so both exception classes (a university / an hour) are carved out explicitly. | When a gate implements a linguistic or formatting rule, enumerate the rule's SURFACES before declaring it covered — numeral vs word, upper vs lower case, symbol vs spelled-out. A partial implementation reads as a complete one in every subsequent audit, because the gate's name says what it checks and nobody re-reads the regex. And extending an old gate is one of the cheapest ways to find defects in content already certified: this one cost twenty minutes and found a grammar error that had survived every review the project has run. |
| L38 | B fill · style gate | **Every gate judged one pack, so none could see FALSE MASTERY — an item whose answer is right every time and guessable every time.** b20's array item only ever compared a×b with b×a, so "they have the same number" was correct on 8/8 exposures across both seeds, one of them a MASTERY slot. The mathematics, the distractors and the rationales were all honest; QG-1..QG-13 had nothing to object to. A child meeting that page twice scores full marks having learned "answer 'the same'", and is promoted. Built `bb-answer-entropy-test.ts` to group items by SLOT IDENTITY (assembly is deterministic, so item k of day d is the same generator on every seed) and measure the keyed answer ACROSS seeds: constant answer, always-max/min, dead options, constant verdict, and the ordinal tell. It found four defect sites in Level B — and then four more in corpus already CERTIFIED: C23's pictograph compare was pinned to "the second display" 60/60 with both other options dead, D1 prevented equality outright (`if (a === b) b += 1`) so `=` was offered on every compare and correct on none, D9's loop REQUIRED the two fractions to differ so "they are equal" could never be keyed, and D3's stem ("one of these is prime and one is composite") contradicted its own third option. | For any generated-assessment system, add a gate at the DISTRIBUTION level beside the per-item one: sample each slot across many seeds and ask whether the answer can be reached without the intended work. Group by slot identity, never by prompt matching — deterministic assembly gives exact like-for-like with no heuristics to tune. And distinguish the two never-correct cases by the L36 test: an option that is FALSE UNDER ANY DRAW is a named misconception held up for rejection and must stay dead (declare it with its proof — `DECLARED_LURES`), whereas an option that is merely never drawn is a defect, fixed by widening the draw (b05, b13, D1, D9) or reframing the question (b18's "counting in tens" could not be unique because every multiple of ten is also reached by twos and fives). A dead option is worse than no option: it teaches its own elimination and collapses a three-way page to a coin flip. |
| L39 | B fill · style gate | **Measure the judge's structural claims before acting on them — and keep the judge, because it sees a class the measurement cannot.** The Level-B style gate reported six generators whose answer was constant across every exposure. Measuring each over 200–300 seeds: b20 real (always a transpose), b12 real (800/800), b05/b13 real (dead option), b18 real as a dead option but not as stated — while **b6 was 404/396 true-vs-false and b21 was 156/144 cubes-vs-paperclips**, both sampling artifacts from four exposures, and b21's author had documented the rotation correctly. Acting on all six would have "fixed" two working generators and rewritten a truthful doc comment to match a false finding. The reverse also held: b12's defect was that the answer was always the SECOND event named — the keyed text varied across seven different events every seed, so answer-text variety looked healthy, and only reading the page caught that what was constant was the answer's RANK. That became the gate's `ORDINAL_TELL` detector, which then independently found b05 keying the FIRST sum listed in 100% of draws — worse than the judge's own account of it. | Two directions, one rule: an LLM judge's numeric or structural claim is a HYPOTHESIS with a sample size, so re-derive it in code before editing anything — at four exposures a 50/50 rotation looks constant about 7% of the time, and a plausible false positive costs a working generator. Equally, when a judge finds something the deterministic gate missed, ask what CLASS it belongs to and encode that class rather than patching the instance; the relational invariant (always the Nth thing named, always the bigger addend) is invisible to any check that only measures answer-text variety. The two gates are complementary in both directions — the reader finds the class, the measurement sizes it and refutes the noise. |
| L40 | B fill · prose pass | **One shared helper can put a whole level over a content law, and the per-week reports all look like per-week problems.** Eight Level-B weeks failed the 15-word sentence ceiling. The largest single cause was not in any week file: `withEstimateFirst` prefixed every metacognition probe with `"Make a call before you work it out — "` and appended `"Decide, then solve:"`, and because an em-dash and a colon are not sentence enders to a splitter, that welded **eleven words onto the first sentence of every probe item in the level**. Fixing the one helper cleared offenders in B6, B12, B15, B16 and B20 at once; a second shared file (`lib/clock.ts`) cleared ~90% of B17 with no edit to b17.ts at all. The corpus sweep then showed the same law failing in **all 24 Level-C weeks**, up to a 60-word sentence, and in A1/A2 — invisible for the whole program because nothing measured it and the style gate's band-register criterion only samples. | When a content law is violated in many units at once, look for the SHARED code path before editing the units — per-unit reports of a shared defect read exactly like independent defects, and fixing them one at a time both costs N times as much and leaves the helper poisoning the next week written. Corollary for prose laws specifically: they need a sweep, not a judge. An LLM gate samples prose and reports "some over-length sentences"; only a script holds every week to a number, and the number turned out to be 0.00% achievable (five weeks measure exactly that) rather than aspirational. And exempt accessibility text from any brevity law explicitly — the first version of the gate ranked two weeks worst in the level for a 22-word clock description that is exactly what a screen-reader child has instead of the picture. |
| L41 | B fill · prose pass | **The estimate-first probe had a 70/30 answer and no gate could ever have caught it, because a probe has no answer key.** `withEstimateFirst` asks the child to commit ("will the coins carried home be worth more than one dime, or less?") before solving; b16 drew the change from `r.int(3, 25)`, giving 7 outcomes below a dime against 16 above, so a child who always answered "more" was right about 70% of the time. The new answer-entropy gate is structurally blind to it — it measures items with CHOICES and one keyed correct, and a probe is prepended prose. It was found by a person reading the draw. Fixed by drawing the SIDE first (`overADime`, then a range on that side), which also let the "exactly one dime" special case fall out of the ranges instead of needing a remap. | Every gate has a coverage boundary, and the honest move is to write it into the gate's own header rather than leave a PASS looking total (the L30 rule, applied to a gate I had just written). For this program the boundary is: no keyed answer means no distribution check — metacognition probes, free-entry numeric answers, and always/sometimes/never claims all need a human to read the draw and ask "what could a child guess?". More generally: when you add a scaffold whose whole purpose is to make a child COMMIT to a judgement, the judgement must be close to a coin flip, or the scaffold teaches the guess instead of the commitment. |
| L42 | B fill · gate calibration | **A new gate's first PASS and its first findings are both untrustworthy, and it took four rounds of reading the output to make this one honest.** The sequence, in order: (1) it reported 12 real mastery defects — genuine. (2) After those were fixed it said PASS across 64 weeks, and `ALWAYS_MAX` had never fired once anywhere, which is what gave it away: the numeric check required EVERY option to parse as a number, so it skipped exactly the shape the detector existed for — two numbers and one verbal option (`["45", "both are composite", "29"]`). (3) Fixing that plus a filter bug — `CERTIFYING` matched on `it.variant ?? it.kind`, neither of which is ever set, so every day slot silently fell outside the failure count — turned PASS into **380 findings**, which is useless in the other direction. (4) Two false-positive classes then had to be named and excluded: comparison items ("Which is greater: 0.6 or 0.40?") key the extreme BY DEFINITION, and the house rule "name every option in the prompt" means an `Always, sometimes, or never true?` stem contains all three option words in its first five, so ranking them measured boilerplate and fired on all twelve Level-B ASN items at 100%. Final honest state: **0 mastery tells, 23 actionable teaching tells**, each with a concrete fix. | A gate is not finished when it runs; it is finished when someone has read its output and can defend every line. Budget for that. Two specific traps worth carrying forward: **a filter keyed on a field that is never set reads exactly like a filter that finds nothing** (the L37 shape, and it hid 28 real tells behind a green PASS — so when a detector never fires across a whole corpus, suspect the detector before congratulating the content); and **a detector must exclude what the question itself guarantees** — tautologies and stem boilerplate produce confident, wrong findings, which is how a gate loses the authority to block anything (L35). Prefer separating BLOCKING slots from REPORTED ones over lowering a threshold: mastery slots certify a child, day slots only teach, and collapsing that distinction forces a choice between 380 blockers and switching the gate off. |
| L44 | C prose pass | **A gate's exemption list needs the same scrutiny as its detection list — the 4th reads-wrong-field instance hid the corpus's worst surface.** The readability gate exempted `hook`/`whyBeforeHow`/`summary` as "teacher framing", but LessonRoom.tsx assembles its spoken segments as [hook, whyBeforeHow, ...script, summary] — Ms. Wren reads all three TO the child. Under the honest gate: whyBeforeHow was 81% over the ceiling in Level C, and 11 LIVE Level-B weeks failed (2.3–4.4%) that had certified green. Fixing the gate + the prose brought B and C to 0.00%. Corollary found during the B repair: 8 of those 11 whyBeforeHow blocks are also adult-VOICED ("Ask a six-year-old what four and three make…") — spoken to the child, about the child. Length was half the defect; the register half is an open design question (re-point the prose, or stop speaking whyBeforeHow). Level C's register was confirmed clean by 6 independent judges. | Before trusting a gate's exemption, verify who CONSUMES the exempted surface in the shipped UI. A surface is child-facing if any renderer speaks or shows it to the child, regardless of what the schema comment says it is for. |
| L45 | C reader eval | **The evaluation protocol's own seed pair was correlated, so its rotation leg under-tested rotation.** Seeds 11 and 202 produce correlated early draws for small ranges (three of four puzzles drew identically at both seeds; joint probability ~1e-4 under independence; xmur3/mulberry32 seeding) — found by a judge replaying the streams. Seed 33 diverges properly and replaced 202. Related: the sprint stream seed is `streamRng(packSeed,'fs')`, week-INDEPENDENT, so every mult-sprint week serves the byte-identical sprint sequence at a fixed pack seed (latent live only because bb_week_state draws a fresh random seed per week). | Verify a protocol's seed pair is actually independent before using it to test rotation. Derive per-week streams from (packSeed, week), not packSeed alone — or document why the collision is acceptable. |
| L46 | C reader eval | **A realizer that silently ignores unknown params turns labels into lies, and one of those lies was a forward leak inside a timed sprint.** `mult_facts_v1` reads ONLY `factorRange` (defaulting [2,9] for BOTH factors); week files passed `{tables:[2,5,10]}` that were silently ignored, and c09/c10's `[2,10]` served ×6–×9 facts first taught in C11/C12 — untaught facts under time pressure, contradicting the weeks' own FACT FIDELITY headers, past every deterministic gate. Six weeks' sprint labels were false as served; c24's promised division facts the template cannot emit. All repaired (taught-only ranges + honest labels), REJECT→ACCEPT on re-judge. | A generator's params surface must REJECT unknown keys, not ignore them. Sprint labels need a deterministic label-vs-servable-set check: enumerate what the params can emit and diff it against the skill text and the taught-fact ledger. |
| L47 | C reader eval | **~2,900 hand recomputations across 24 weeks found ZERO wrong numbers — and 20+ claim-level defects, including a false core rule standing beside correct arithmetic.** C12 taught "a ten-count writes the NUMBER OF GROUPS with a zero behind it" in five child-facing sites; 10×7=70 writes 7, what one group HOLDS — every worked number beside the sentence was right, so no numeric audit could object, and a child learning the sentence learned a false rule (the exact groups-vs-group-size confusion the week's own mistakeBank trains against). Caught only because judges were instructed to apply each stated RULE to the pack's own worked examples and check its REFERENTS. Same pattern at week scale: isomorphNotes freshness claims false in 12 of 18 weeks checked, stale sprint labels, a rationale claiming "one group short" for a value one group over. | "Recompute the arithmetic yourself" is necessary but not sufficient — add "recompute the RULE": apply every stated generalization to the pack's own examples and name what each noun phrase refers to. The numbers being right proves nothing about the sentence beside them. |
| L43 | B4 · rank tell | **A rule written as one instance invites the MIRROR defect, and the author who obeys it carefully is the one who builds it.** Kit §E2.11 said "every distractor undershooting" is the trap, because that is the form the first 23 findings took. b04's author read it, defended against it properly, and made the two distractors BRACKET the answer on every draw — documenting that as a feature, since neither "pick the biggest" nor "pick the smallest" then works. Both true. But bracketing on every draw makes the answer the MIDDLE number every time, and "pick the middle" is exactly as free — 100% of draws, in a mastery slot. My entropy gate passed it, because ALWAYS_MAX and ALWAYS_MIN between them do not cover the middle. It was caught only because the author measured their own rank distribution and reported it plainly. Generalising the detector to CONSTANT_NUMERIC_RANK at any position then immediately found the same shape in **b13** — a week already certified AND style-gate ACCEPTed — and in **A1/A2's "circle the number that shows how many"**, where `n−1, n, n+1` made "circle the middle number" a free pass in a mastery slot for the youngest children in the product. | State a content rule as the INVARIANT, not as the instance you happened to find first: "the answer must not sit at a fixed rank" rather than "distractors must not all undershoot". A rule phrased as one instance is a rule an author can satisfy while reproducing the defect in a form the rule does not name, and the more conscientious the author the more likely that is. The corresponding gate rule: when a detector covers the ends of a range, ask what covers the interior — and prefer one general measure (rank) over two special cases (max, min). The fix that works for the whole class is having **enough honest wrong values that the PAIRING can rotate**; two extra named misconceptions were sufficient in all four weeks, so the answer lands low, middle and high in turn. |
| L48 | A fill · exemplar | **The most dangerous surface was the one no gate read: at band A the figure's `alt` is SPOKEN, and it was reading the answer out before the question.** Band A is `audioFirst` — the child cannot read, so every prompt autoplays (`CheckRunner.tsx:128`, `TreasureChest.tsx:175`, `autoplay={band === 'A'}`), and `speakablePrompt` prepends the scene and **prefers `figure.alt` over the `[image: …]` bracket**. Most `earlynumber` generators set `alt` to a string naming the count the item asks for, so a four-year-old heard *"**5 ducks in a row.** Count the ducks. How many?"* — on essentially every counting item in the level. Every deterministic gate passed it: QG-13 proved the picture agreed with the answer, which is exactly what made the alt *helpful* (L33 — the dangerous figure is not a wrong one, it is a helpful one; ask what it lets the child SKIP). The fix went where the audio reads from, not where the prose lives: the bracket stays byte-identical (it is what QG-1/QG-4 sign for operand freshness and pack identity, L29) and only `alt` changed, to describe layout and never the asked-for quantity. Numbers that are GIVENS stay — a build task's "draw 3 counters", a frame's capacity, a story that states its own count. Second-order find: removing a count from a Day-4 story then MADE the new gate fail, because those alts had been sheltered behind the given-exemption. | Ask of every content surface: **who CONSUMES this, and in what modality?** (L44 asked it of an exemption list; this asks it of a field the schema calls accessibility text.) A surface that is read ALOUD to a pre-reader is not decoration and is not a11y garnish — it is the first thing the child hears, and it must be gated as a child-facing surface. New required gate `scripts/bb-spoken-answer-test.ts`: band A FAILS, B–E advisory (their alts describe rich pictures, and failing them would train authors to describe pictures LESS — the harm the readability gate's alt exemption exists to prevent). It ships `--selftest` with 13 fixtures proving every rule fires and every guard holds, because three gates in this project have now been found reading fields that are never set. |
| L49 | A fill · pre-flight | **Three of 22 weeks were unbuildable as specified, and asking the question ONCE across the whole level cost minutes where discovering it per-week would have cost three agent runs.** The 08-09 handoff named one blocker (A5's conservation figure: `CountersFig` derives a single pitch from the longest row, so more counters always occupy more width). Applying that same question — *can the renderer draw what this recipe's discrimination names, and at band A can it be SPOKEN?* — to all 22 remaining recipes before launching any agent found two more. **A3** ("correct 3 vs mirrored 3") is blocked three ways over: no primitive draws a numeral glyph, a reversed 3 is not an ASCII character so it cannot be an option string, and an alt describing it would have to name the digit it asks about. **A8** (above/below/beside, attribute sort) has no generator among the 27 and no primitive expressing a relation BETWEEN two objects; `sortAndTell` sorts by count, not attribute. Equally valuable were the three that looked blocked and were not: `patternNext` does emit a figure (the "shape-pattern strip" entries in `bb-verify-packs`' debt list are dead ones describing an A2 that no longer exists), `compareMeasure` sidesteps the missing balance primitive honestly by comparing through a common unit, and `solidChoice` is deliberately text-and-choice with a documented pairing rule. A5 was then unblocked additively — an optional per-group `spread` multiplying one row's SPACING (never its counter size, or the illusion under test changes). | Before fanning out over N units, run the **feasibility sweep across all N first**, and write down both verdicts — blocked AND cleared. A per-unit author discovers a structural blocker at the most expensive moment, having already read everything; three authors discover it three times; and each is tempted to quietly substitute rather than escalate, which is how a recipe silently loses the misconception it is named for. Corollary on the allowlist that hid it: `FIGURE_DEBT` still permits four `A2-*` ids that no longer exist. **An allowlist entry that can never match is the mirror of a detector that never fires** — it reports nothing while silently pre-authorising whatever reuses those ids. Audit permit-lists for dead entries the same way detectors are audited for empty sets. |
| L50 | A fill · validation | **Two more gates were reading an empty set, and both were about a gate's ENABLEMENT list rather than its logic — the logic was correct in each case and never ran.** (1) `validator.ts` gated the QG-5 arithmetic re-check on `['exact-numeric','equivalent-numeric','equivalent-fraction','ordered-list']`, omitting `'set'`. Fifteen items corpus-wide validate as `'set'`, four behind a registered template — A12's all-ways-to-make-5 and **three LIVE B16 money combinations** — so their `answerFor` was registered, green, and never called. Proved with a negative control, which is the only reason the finding is worth anything: deleting a member from a generated set answer left `validatePack` at **0 violations**, while the identical corruption on an `exact-numeric` answer fired **2**. All four templated items were then hand-audited across three seeds and agreed, so it was a latent hole, not a live defect. (2) `packGenerator.ts`'s `V2_WEEKS` had never been regenerated after the A1/A2 rebuild, and `bb-verify-packs.ts:65` reads `contract = V2_WEEKS.has(cell) ? 'v2' : 'v1'` — so **the two exemplars the whole level is modelled on were being validated under the v1 contract**, outside the v2 pedagogy preflight, in a commit made the same day. Regenerating the wiring added them and the corpus went 19,732 → 19,958 assertions. Both were found by tooling that already existed (`bb-wire-weeks.ts`) or by an author probing a pin they depended on. | Audit a gate's **enablement list** with the same suspicion as its detection logic. "Which items does this rule APPLY to?" is a separate question from "is this rule correct?", and the first one fails silently in a way that looks exactly like a clean pass — a whitelist missing a validation mode, a week missing from a contract set, a permit outliving what it permitted (L49), a field that is never set (L37/L42). The standing test is now: for every pin you rely on, **corrupt the thing it guards and watch it fail**, and keep a NEGATIVE CONTROL beside it — a probe that fires nowhere proves nothing, and a probe that fires everywhere proves nothing either. Corollary earned the same day: when a regression harness reports a failure, check the harness before the change. A probe forcing `contract: 'v2'` on every cell "found" D17 broken; D17 is a pinned v1 fixture and failed identically on the untouched validator. |
| L51 | A20 · generator bias | **A generator can be guessable and correct and still teach AGAINST the week it serves — and that is a worse failure than a wrong answer.** `compareMeasure` drew `a` from 4-9 and `b` from 2-8, with a tie nudge sending four of its five cases to `a`, so the answer was **the first thing it named on 73.5% of draws** (measured, 6,000 per attribute; identical at weight/capacity/length because the bias is in the draw, not the pool). At band A, where the scene is read aloud, that alone is a page a child scores by tapping whatever the audio names first. But `SCENES.weight` pairs are `bag/ball`, `book/leaf`, `rock/feather`, and the first-named is the heavier-LOOKING thing in each — so the generator **confirmed "the thing that looks heavier is heavier" 73% of the time**, which is the precise misconception A20 exists to unseat. Every gate passed it: the arithmetic was right, the figure agreed with the answer, and the entropy gate exempts comparison items because they key an extreme BY DEFINITION. Found by an author measuring blind habits its own week named. A second defect in the same pools, found while verifying the first: `['long stick', 'short stick']` against "Which one is longer?" — on 74% of draws "tap the one called long" scored full marks without measuring, and on the rest the page contradicted itself ("the short stick measures 8 steps, the long stick 3 … which is longer?"). Fixed: the counts are swapped on a coin flip (73.5% → 50.12%), and things are distinguished by colour, which is orthogonal to the attribute under test. | Two rules. (1) **For every item, name the blind habit the WEEK exists to punish and measure whether it wins.** "Is this guessable?" is the weak form; the strong form is "does guessing it reward the exact misconception I am teaching against?" A gate cannot ask this — it does not know what the week is for — so it belongs in the author's brief and in the report, as a measured rate beside its chance floor. (2) **Never name a thing after the attribute the item asks about.** A "long stick" in a longer/shorter item leaks the answer when it is right and is absurd when it is wrong; distinguish by a property orthogonal to the question. Corollary for exemptions: the entropy gate's comparison carve-out is correct and it is also a blind spot — an exempted item is unmeasured, not proven safe. |
